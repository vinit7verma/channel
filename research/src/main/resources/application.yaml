spring:
  application:
    name: research
  datasource:
  driverClassName : org.hsqldb.jdbc.JDBCDriver
  url : jdbc:hsqldb:mem:myDb
  username : admin
  password : admin

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: [ "health","prometheus", "metrics" ]
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: true
    prometheus:
      enabled: true
server:
  port: 9001